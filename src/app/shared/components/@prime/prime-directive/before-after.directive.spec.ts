import { BeforeAfterDirective } from './before-after.directive';

describe('BeforeAfterDirective', () => {
  it('should create an instance', () => {
    const directive = new BeforeAfterDirective();
    expect(directive).toBeTruthy();
  });
});
